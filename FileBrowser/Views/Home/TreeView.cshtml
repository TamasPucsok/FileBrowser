@using Kendo.Mvc.UI;

    @(Html.Kendo().TreeView()
                .Name("treeview")
               .BindTo((IEnumerable<TreeViewItemModel>)ViewBag.inlineItemTreeData)
               .Events(events => events
                .Select("onSelect")

                )
    )
